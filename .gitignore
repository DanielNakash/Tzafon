# macOS
.DS_Store

# Android / Gradle
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.kotlin/
*.apk
*.aab
*.hprof

# IDE
.idea/
*.iml
.vscode/

# Claude local settings
.claude/settings.local.json

# Emulator screenshots captured during build-and-verify
.screenshots/
.shots/
.build-shots/
.tz-screenshots/

# Pipeline / local build artifacts (not part of the source of record)
PIPELINE_LOG.md
scratch/
android/gradle/gradle-daemon-jvm.properties

# Secrets (Firebase config lands in M9b, never committed)
android/app/google-services.json

# Release signing — keystore and its passwords never leave this machine
android/keystore.properties
*.jks
*.keystore

# Personal scratch folder — never committed
NON COMMIT/
